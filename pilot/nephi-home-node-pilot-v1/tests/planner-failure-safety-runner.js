"use strict";
const assert = require("node:assert/strict"), crypto = require("node:crypto"), fs = require("node:fs"), os = require("node:os"), path = require("node:path");
const { ConversationEngineV2, normalizePlannerOutput } = require("../lib/conversation-engine-v2/engine");
const { plannerJsonSchema, validatePlannerOutput } = require("../lib/conversation-engine-v2/planner-schema");
const { createApp } = require("../server");
const property = { propertyId: "demo_homestay_a", timezone: "Asia/Taipei", rooms: [], commonAnswers: { checkInTime: "15:00" } };
async function engineResult(output) { const logs=[]; const engine=new ConversationEngineV2({planner:{classify:async()=>output},persistence:{getConversationState:()=>null,setConversationState:()=>{},appendMessageLog:(_p,v)=>{logs.push(v);return{reviewId:"r"};}},getProperty:()=>property,availabilityResolver:()=>({availabilityReliable:true,rooms:[]}),listPriceOverrides:()=>[]}); const result=await engine.process({customerId:property.propertyId,channelId:"test",lineUserId:"guest",eventId:String(output),eventTimestamp:1,messageText:"test"}); assert.equal(result.shouldReply,true);assert.ok(result.replyText.length>0);assert.equal(logs.at(-1).decisionReason,"planner_empty_output"); }
(async()=>{
  for(const output of [null,undefined,"not-an-object",{schemaVersion:2}]) await engineResult(output);
  const strict=plannerJsonSchema().properties.tasks.items; assert.ok(strict.required.includes("detailIntent"));assert.ok(strict.required.includes("eligibilityEvidence"));
  const valid={schemaVersion:2,discourse:{relation:"new_request",confidence:1},stateOperations:[],stay:{dateExpression:{rawText:"",kind:"none",anchor:"none"},checkInCandidate:null,checkOutCandidate:null,nightsCandidate:null,guestCountCandidate:null},tasks:[{taskId:"t",type:"policy",sourceText:"check in",detailIntent:"general",requestedOutputs:["answer"],eligibilityEvidence:{kind:"none",sourceText:""},dependsOnStayContext:false,entity:{category:"policy",rawText:"check in",canonicalCandidate:"check_in",confidence:1},confidence:1}],ambiguities:[],missingInformation:[],needsHuman:false,shouldIgnore:false,reason:"test"};
  assert.equal(validatePlannerOutput(valid).ok,true);valid.tasks[0].detailIntent="free_text";assert.equal(validatePlannerOutput(valid).ok,false);const normalized=normalizePlannerOutput({...valid,tasks:[{...valid.tasks[0],detailIntent:"general"},{...valid.tasks[0],taskId:"t2",detailIntent:"free_text"}]});assert.equal(normalized.tasks.length,2);assert.equal(normalized.tasks[1].detailIntent,"general");
  const temp=fs.mkdtempSync(path.join(os.tmpdir(),"planner-failure-safety-")),secret="planner-failure-secret",replies=[];const app=createApp({dataFile:path.join(temp,"store.json"),seedFile:path.resolve(__dirname,"../fixtures/seed.json"),lineChannelSecret:secret,lineChannelAccessToken:"token",conversationDebounceMs:1,lineChannelIdentityGuardRequired:false,conversationPlannerV2:{classify:async()=>null},lineReplyClientFactory:()=>({replyMessageWithHttpInfo:async(body)=>{replies.push(body);return{httpResponse:{status:200}};}})});const running=await app.start(0,"127.0.0.1");
  try { const payload=JSON.stringify({destination:"line",events:[{type:"message",webhookEventId:"planner-null-event",replyToken:"reply-token",timestamp:1,source:{userId:"guest"},message:{type:"text",id:"m1",text:"test"}}]}),signature=crypto.createHmac("sha256",secret).update(payload).digest("base64");assert.equal((await fetch(`${running.url}/api/test-line/webhook?customerId=demo_homestay_a`,{method:"POST",headers:{"content-type":"application/json","x-line-signature":signature},body:payload})).status,200);await new Promise(r=>setTimeout(r,50));assert.equal(replies.length,1);assert.ok(replies[0].messages[0].text.length>0); } finally {await app.stop();fs.rmSync(temp,{recursive:true,force:true});}
  console.log("planner failure safety: PASS");
})().catch(error=>{console.error(error.stack||error);process.exitCode=1;});
